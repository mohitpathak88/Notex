<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/header.ejs") %>
<body>
    <%- include("./partials/nav.ejs") %>

    <div class="blogs content">
     <h2>All Notes</h2>     
    <!-- We have access to every part of the blog including the id in ind-->
     <% if (notes.length > 0) { %>
        <% notes.forEach(note => { %>
        <a class="single" href="/notes/<%= note._id %>">    <!-- So that each blog displayed opens up a different route with its own id as
           the route parameter-->
          <h3 class="title"><%= note.title %></h3>
          <p class="snippet"><%= note.snippet %></p>
        <% }) %>
        </a>
      <% } else { %>
        <p>There are no notes to display...</p>
      <% } %>
    </div>

    <%- include('./partials/footer.ejs') %> 
</body>
</html>